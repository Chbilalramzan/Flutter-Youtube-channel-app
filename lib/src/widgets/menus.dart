import 'package:flutter/material.dart';

class Menus {
  final String id;
  final String label;
  final Color colour;

  const Menus({
    @required this.id,
    @required this.label,
    this.colour,
  });
}
